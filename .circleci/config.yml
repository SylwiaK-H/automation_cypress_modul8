version: 2.1

jobs:
  test:
    docker:
      - image: cimg/node:20.11-browsers
    steps:
      - checkout
      - run: npm install
      - run: npx cypress run

workflows:
  test-workflow:
    jobs:
      - test
